@model IEnumerable<TypeOfActive>
@{
    ViewData["Title"] = "Index";
}
<partial name="_Notification"/>
<h1>Таблица <Тип Актива> </Тип></h1>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-12 text-end">
            <a asp-controller="TypeOfActive" asp-action="Create" class="btn btn-primary">
               <i class="bi bi-plus-circle"></i> &nbsp; Создать Тип Актива
            </a>
        </div>
    </div>
    <br /><br />

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Первичный ключ
            </th>
            <th>
                Тип Актива
            </th>
            <th>
                Действие
            </th>
        </tr>
    </thead>
    <tbody>
            @foreach (var obj in Model)
            {
                    <tr>
                            <td width="20%">
                        @obj.TypeOfActive_id
                        </td>
                            <td width="50%">
                        @obj.Type
                        </td>
                       <td width="30%">
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="TypeOfActive" asp-action="Edit" asp-route-id="@obj.TypeOfActive_id"
                        class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>Редактировать</a>
                                    <a asp-controller="TypeOfActive" asp-action="Delete" asp-route-id="@obj.TypeOfActive_id"
                        class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Удалить</a>
                            </div>
                        </td>
                    </tr>
            }
    </tbody>
</table>
</div>